decisione(normale).
decisione(furioso).
decisione(conservativo).

profiling(X)|notProfiling(X):- decisione(X).

%solo una scelta per volta
:-#count{X:budget(X)}!=1.
:- #count{X:profiling(X)}!=1.
:- notProfiling(normale),budget(X),X>=10000 ,X<=17000.
:-notProfiling(furioso),budget(X),X>17000.
:-notProfiling(conservativo),budget(X),X<10000.